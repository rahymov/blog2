<%= @posts.each do |post| %>
	<div class="post_wrapper">
		<h2 class="title"><%= link_to post.title, post %></h2>
		<p class="date">
			<span><small><%= post.created_at.strftime("%B %d, %Y") %> </small></span>
		</p>
	</div>		
<% end %>